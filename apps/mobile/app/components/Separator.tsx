import React from 'react';
import { View } from 'react-native';

const Separator = () => {
  return (
    <View
      style={{
        borderBottomColor: 'grey',
        borderBottomWidth: 1,
        marginVertical: 20,
      }}
    />
  );
};

export default Separator;
