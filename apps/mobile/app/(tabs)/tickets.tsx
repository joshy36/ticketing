import { View, Text } from 'react-native';

const Tickets = () => {
  return (
    <View className="flex-1 bg-black">
      <Text className="text-white font-bold text-3xl">Test</Text>
    </View>
  );
};

export default Tickets;
